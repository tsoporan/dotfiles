# ==============================================================================
# Antidote plugin list
# ==============================================================================
#
# This file is in git. Clones and the generated ~/.zsh_plugins.zsh are not.
# Config: ~/.config/antidote/config.zsh (stowed from zsh/.config/antidote/).
#
#   just zsh-plugins-outdated   # dry-run
#   just zsh-plugins-update     # pull plugins + antidote, snapshot SHAs
#
# Plugin syntax:
#   repo/plugin                     - Standard load
#   repo/plugin kind:defer          - Deferred loading (faster startup)
#   repo/plugin path:subdir         - Load specific subdirectory
#   repo/plugin kind:fpath          - Add to fpath only
#
# ==============================================================================

# Syntax highlighting (must load before autosuggestions)
zdharma-continuum/fast-syntax-highlighting kind:defer

# Oh-My-Zsh plugins
ohmyzsh/ohmyzsh path:plugins/fancy-ctrl-z

# Completions
zsh-users/zsh-completions path:src kind:fpath
belak/zsh-utils path:completion

# Autosuggestions (history search handled by atuin)
zsh-users/zsh-autosuggestions kind:defer
