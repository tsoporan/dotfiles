.claude/
*.bak
*.bak.*

# Machine-local working notes. Never push.
NOW.md

# GPG: only conf files are tracked. Never add keyrings or agent state.
gnupg/.gnupg/**
!gnupg/.gnupg/gpg.conf
!gnupg/.gnupg/gpg-agent.conf
